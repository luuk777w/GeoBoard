export interface LoginViewModel {
    username: string;
    password: string;
    rememberMe: boolean;
}

export interface RegisterViewModel {
    username: string;
    email: string;
    password: string;
}
